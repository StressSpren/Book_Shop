# How to Start a Django project

mkdir project
cd project

# VM

python -m venv .venv
.venv\Scripts\Activate

# installing django on VM

python -m pip install django
django-admin startproject django_project .
python manage.py migrate
python manage.py runserver

# Directing the django urls to the django.contrib.auth.urls 

cd django_project

code urls.py

urlpatterns = [
    path("admin/", admin.site.urls),
    path("accounts/", include("django.contrib.auth.urls")),  
    
]

# Setting up Login Page

## Create -> 

/Project/templates/registration/login.html

## Inisde the html document -> 

<h2>Log In</h2>
<form method="post">
  {% csrf_token %}
  {{ form }}
  <button type="submit">Log In</button>
</form>


## /django_project/settings.py Include ->


TEMPLATES = [
    {
        ...
        "DIRS": [BASE_DIR / "templates"],  
        ...
    },
]

### At the bottom of the page

LOGIN_REDIRECT_URL = "/"  


# Add to urls.py

from django.views.generic.base import TemplateView

urlpatterns = [
    ...

    path("", TemplateView.as_view(template_name="home.html"), name="home"),

    ... 
]


# Create two files 

/Project/templates/home.html and /Project/templates/base.html


# Creating a form in html file

<!-- <form action="{% url 'logout' %}" method="post">
    {% csrf_token %}
    <button type="submit">Log Out</button>
  </form> -->



# TODO: 

Add a details page for book, done by setting the urls to submit the image seleced into the url to direct to a details page

Design a presentable method to display books

set the the permissions to read and access.

clean up visuals on login screen and register screen

